/**
 * Database placeholder module.
 * Reserved for future SQLite / SpatiaLite / DuckDB integration.
 */

/**
 * Returns true if a database connection is configured.
 * Currently always returns false (no DB in template).
 */
export function isDatabaseConfigured(): boolean {
  return false;
}
